# Rental Marketplace Client (MVP)

## Quick start
```bash
# In the client folder
npm install
npm run dev
```

By default it expects the API on `http://localhost:4000`. To point elsewhere, create a `.env` file with:

```
VITE_API_BASE=http://your-api:4000
```
